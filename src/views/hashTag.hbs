<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="{{ meta.charset }}" />
    <title>{{ title }}</title>
    <meta name="description" content="{{ meta.description }}" />
    <link rel="shortcut icon" href="{{ staticBasePath }}favicon.png" type="image/png" />
    <link rel="stylesheet" href="{{ staticBasePath }}styles/index.css" media="screen" />
    <link rel="stylesheet" href="{{ staticBasePath }}styles/header.css" media="screen" />
    <link rel="stylesheet" href="{{ staticBasePath }}styles/adventures.css" media="screen" />
    <link rel="stylesheet" href="{{ staticBasePath }}styles/hashTag.css" media="screen" />
</head>
<body>
{{> header }}
<h1 class="hash-tag">#{{ hashTag.textRu }}</h1>
{{> adventures}}
</body>
</html>
